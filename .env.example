# Example environment variables for this repository.
# Copy to `.env` and fill values before running apps locally.

# ---------- Core Services ----------
# Postgres connection URL (Prisma uses this)
# e.g. postgresql://user:password@localhost:5432/education
POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/education

# Redis connection URL (queues/sessions/cache)
REDIS_URL=redis://localhost:6379

# Meilisearch host and API key
# e.g. http://127.0.0.1:7700
MEILI_HOST=http://127.0.0.1:7700
MEILI_API_KEY=changeme

# JWT secret (server-only)
JWT_SECRET=changeme

# Web origins (CORS/CSRF)
WEB_ORIGIN=http://localhost:3000
SSU_WEB_ORIGIN=http://localhost:3002

# Mux Video/Uploads (placeholders)
MUX_TOKEN_ID=your_mux_token_id
MUX_TOKEN_SECRET=your_mux_token_secret
MUX_SIGNING_KEY_ID=your_mux_signing_key_id
MUX_SIGNING_KEY_PRIVATE=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----

# Payments (Razorpay)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# ---------- App Flags ----------
# Use in-memory DB for tests (0/1)
USE_IN_MEMORY_DB=0

# API port
PORT=3001

# Base URL for tests
BASE_URL=http://localhost:3001/api

# Notes:
# - Keep secrets server-only. Do not expose to client without NEXT_PUBLIC_ prefix.
# - Prefer setting secrets in CI/CD secret store for deployments.
